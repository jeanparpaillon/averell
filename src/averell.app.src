%%-*- mode: erlang -*-
{application, averell,
 [
  {applications, [
		  kernel,
		  stdlib,
		  cowboy,
		  cowlib,
		  cowboy_cors,
		  getopt
		 ]},
  {description, "Averell is a simple file HTTP server"},
  {modules, [
	     averell,
	     averell_cors
	    ]},
  {vsn, git}
 ]}.
